name: g_sync
description: >
  A Mass Data Transaction package for Flutter/Dart. 
  Provides offline-first sync with Hive, network requests, and file handling.
version: 0.0.1+1
homepage: https://github.com/asl-softtech/flutter-dart-packages/tree/main/g_sync
repository: https://github.com/asl-softtech/flutter-dart-packages/tree/main/g_sync

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  http: ^1.6.0
  path: ^1.9.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.10.5
  hive_ce_generator: ^1.10.0
  mocktail: ^1.0.4

flutter:

